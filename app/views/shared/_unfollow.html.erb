<%= form_for(current_user.relationships.find_by_followed_id(@user.id),
              :html => { :method => :delete },
              :remote => true) do |f| %>
  <div class="follow_button">
    <%= f.submit "Unfollow" %>
  </div>
<% end %>

